// TODO: You — Load and manage the GLB knee model
// Use useGLTF from @react-three/drei to load '/models/knee.glb'.
// Iterate over model nodes (useGLTF result) and render each mesh.
// Filter visibility using visibleStructures from useAppStore.
// Handle click events to call setSelectedStructure.
// Apply emissive highlight to selectedStructure mesh.

export default function KneeModel() {
  // TODO: const { nodes, materials } = useGLTF('/models/knee.glb');
  // TODO: const { visibleStructures, selectedStructure, setSelectedStructure } = useAppStore();
  return null;
}
