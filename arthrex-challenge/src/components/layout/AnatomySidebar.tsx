// TODO: Teammate B — Build the left anatomy sidebar
// Should render a list of anatomy structures with toggle switches.
// Use visibleStructures, toggleStructure, showAll, hideAll from useAppStore.
// Use anatomyData from src/data/anatomyData.ts for labels and categories.

export default function AnatomySidebar() {
  return (
    <aside className="w-64 bg-surface border-r border-slate-700 p-4">
      <p className="text-slate-400">AnatomySidebar placeholder — Teammate B</p>
    </aside>
  );
}
