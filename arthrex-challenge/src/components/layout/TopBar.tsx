// TODO: Teammate B — Build the app header
// Should include: Arthrex logo/wordmark, view mode tabs (Explore / Procedure),
// and a mode toggle (Surgeon / Patient).
// Use viewMode and setViewMode from useAppStore.
// Use mode and setMode from useModeContext.

export default function TopBar() {
  return (
    <header className="h-14 bg-surface border-b border-slate-700 flex items-center px-4">
      <p className="text-slate-400">TopBar placeholder — Teammate B</p>
    </header>
  );
}
