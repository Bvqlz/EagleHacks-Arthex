// TODO: Teammate A — Build the right procedure walkthrough panel
// Should display the current step from procedureSteps.ts with prev/next navigation.
// Use currentStep, setCurrentStep, totalSteps, viewMode from useAppStore.
// Use useContent hook to get mode-appropriate text.
// Render ProgressBar and step content (title, description, instruments list).

export default function ProcedurePanel() {
  return (
    <aside className="w-80 bg-surface border-l border-slate-700 p-4">
      <p className="text-slate-400">ProcedurePanel placeholder — Teammate A</p>
    </aside>
  );
}
